# Run this to checkout the latest bdb benchmark git repo and to fetch the required data from s3

all: benchmark data
benchmark:
	git clone https://github.com/craiig/benchmark

data:
	make -C ./data/ all
