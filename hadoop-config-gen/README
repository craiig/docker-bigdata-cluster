These tools generate a hadoop configuration for use by other non-master hadoop setups
(the master hadoop setup generates it's own config)
At the moment, only the /local-hadoop/ system uses this configuration.
